#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../dom-compare-temp/bin/domcompare" "$@"
  ret=$?
else 
  node  "$basedir/../dom-compare-temp/bin/domcompare" "$@"
  ret=$?
fi
exit $ret
